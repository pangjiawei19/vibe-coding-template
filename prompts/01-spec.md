# Spec

## 1. 角色设定与目标对齐

```markdown
你好！现在的任务是：我们要从零开始设计并新增一个量化交易系统的功能。

你现在不仅是资深的 Python 工程师，更是一位经验丰富的产品经理。我有一个初步的想法，需要你通过向我提问，帮助我澄清需求、挖掘边缘场景，最终目标是共创一份高质量的 `spec.md`。

我的初步想法是：

1. 我们在 backend 中增加一个量化交易模块，先实现整个功能的后端部分，不用关心前端。因为只做后端，所以使用的时候是不是可以通过直接运行 python xxx 去启动相关的功能
2. 该系统支持多个量化交易策略，目前包括：趋势策略、均线策略、布林带策略、均仓策略
3. 整个系统的核心功能有：回测功能（基于历史数据验证策略效果）、投资决策（生成指定日期的投资建议）
4. 投资和回测等都是以天为单位，即每个标的每日的收盘价。不用考虑一天内的变化，但未来可能要迭代到支持更细粒度的时间数据
5. 要有业绩评估指标体系，包括年化收益率、年化波动率、夏普比率、最大回撤、卡玛比率等
6. 数据源使用 akshare，获取的数据最好能存储到数据库中，这样不需要每次执行时都去获取
7. 关于 akshare 的使用，要使用最准确地 api。我们的分析针对的标的有股票、指数、etf 等，好像 akshare 有不同的 api，像 stock_zh_index_daily 是不是就可以直接用
8. 有几个问题需要考虑解决方案：
  a. 指数的值往往都很大，计算时要考虑超限的问题
  b. 对于股票之类的标的，好像有因为分红导致净值下跌，这在做回测的时候就有问题了，看看怎么办

请开始你的提问。
```

## 2. 和 AI 交互，不断澄清需求

## 3. 最终确认与生成指令

```markdown
**现在，所有的需求都已清晰。**

请扮演“需求编译器”，执行以下操作：
1.  在项目根目录下创建一个名为 specs/001-core-functionality/ 的目录。
2.  在该目录下创建 spec.md 文件。内容必须包含：
    *   **用户故事**（核心需求背景是什么）
    *   **功能性需求**（涵盖我们讨论的所有细节）
    *   **非功能性需求**（架构解耦、错误处理）
    *   **验收标准**（列出具体的测试Case）
    *   **输出格式示例**（包含你刚才设计的Markdown结构和Frontmatter）
请直接执行工具生成文件。
```

重要：**审查 spec.md**

## 4. 生成初始目录结构与辅助文档

```markdown
非常好。基于这份 `spec.md` 和我们的 `constitution.md`（特别是关于包内聚的原则），请为这个功能设计详细的包结构。
```
